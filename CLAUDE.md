# Folder Bot — 리포 작업 규칙

**Folder Bot** = 내 Mac mini 위에서 사는, 폴더마다 하나씩 붙는 AI 동료들.
기획 정본은 `docs/PRD.md` — 설계가 바뀌면 **PRD 에 먼저** 적고 코드를 고친다.
**할 일 정본은 볼트 `PARA/3. Area/제품_FolderBot/todo.md`** — 리포 안 `todo.md` 는 스텁이다(2026-09-22 Dave 지시). 세션마다 먼저 읽고, 끝낸 줄은 `- [x]` + `→ 처방`.

---

## 1. 🔴 목업 먼저, 제품은 그 다음 (2026-09-13 Dave 확정 — **최우선 계약**)

> Dave: *"앞으로 항상 목업 먼저 그리고 제품 만들 거야."*

**화면에 보이는 것을 바꾸는 일은 코드를 쓰기 전에 목업으로 먼저 보여 준다.**

- 도구는 `/design` (Claude Design 캔버스). 실제 쓰이는 **크기와 줄 안에서** 그린다 —
  아이콘만 떼어 놓고 보면 다 괜찮아 보이고, 줄 안에 넣어야 부딪히는 게 보인다.
- 방향이 갈리는 지점은 **안을 2~4개** 내고 각각에 **솔직한 대가**를 적는다.
  내 favorite 에만 근거를 붙이면 그건 고르는 게 아니라 짜고 치는 것이다.
- Dave 가 고르면 **그 안대로만** 만든다. 고르지 않은 안은 산출물에 남기지 않는다.
- ⛔ **«간단해 보여서» 를 이유로 건너뛰지 않는다.** 이 규칙이 생긴 이유가 그거다 —
  표식을 아이콘 모서리에 붙이는 «한 줄» 이 상태 배지와 겹쳐 두 라운드를 잡아먹었다.

**목업 없이 바로 고쳐도 되는 것** (범위를 넓히지 말 것):
버그 수정 · 이미 승인된 목업의 구현 · 보이지 않는 것(호스트·API·성능) ·
Dave 가 «이대로 해» 라고 지정한 변경.

## 2. 라운드 리추얼

| 무엇 | 규칙 |
|---|---|
| `docs/PRD.md` | 라운드마다 **맨 아래에** `**vX.Y.Z (날짜) — 제목**:` 한 문단. 결정·근거·금기를 적는다 |
| 커밋 | 한국어. 무엇을 왜 바꿨는지. 라운드 단위 |
| 스모크 | **새 계약에는 검사를 남긴다.** 되돌리면 빨개지는지 직접 확인하고 적는다 |

⚠ **«고치기 전 판에서 실제로 빨개지는가» 를 확인하지 않은 검사는 검사가 아니다.**
초록인 검사를 넣고 «회귀 방어 완료» 라고 적는 것이 가장 흔한 거짓말이다.

## 3. 검증 · 배포

```sh
npm run qa      # tsc + vitest + build + 스모크(헤드리스 UI 포함). 배포 전 필수
npm run qa:mac  # 맥에서만 — 호스트 번들 → 클립보드 → 실제 Electron 앱(유리·nowrap·독·IPC 복사·창 자리·딥링크). test/mac-qa.mjs 머리말
```

- 🍎 **맥이 아니면 못 재는 것은 `qa:mac` 이 잰다** — 픽스처 볼트·스텁 CLI·`FOLDERBOT_QA=1`(프로토콜·로그인 항목·업데이터 끔)·`FOLDERBOT_USER_DATA`(userData 격리)로 Dave 의 실 앱·실 볼트를 안 건드린다. `qa` 가 도는 동안 따로 돌리지 않는다(둘 다 `dist/` 를 쓴다).

- main 에 푸시하면 CI(`.github/workflows/desktop.yml`)가 **자동으로** `desktop-v<run_number>` 를 낸다.
  앱은 그 릴리스를 스스로 받아 재시작한다. 사람이 DMG 를 나르지 않는다.
- ⛔ **빈 커밋을 밀어 CI 를 깨우지 않는다.** 워크플로에 `workflow_dispatch:` 가 있으므로 수동 실행이 정답이다.
- CI 가 `queued` 인 채 `updated_at` 이 안 변하고 **job 이 0개**면 워크플로를 의심하기 전에
  **githubstatus.com 을 먼저 본다** (2026-09-13 실사고 — GitHub 장애였다).
  그때의 우회로는 `PARA/1. Inbox/2026-09-13_Folder-Bot_맥에서-직접-빌드.md`.

## 4. 아키텍처 계약

- **`src/core` 는 순수하다** — Electron·DOM 에 의존하지 않는다. 호스트와 화면이 같은 타입을 읽는다.
- **뺄셈은 한 곳에서만.** 사용량의 «남은 양» 같은 계산은 호스트가 한 번 하고 화면은 받아 그린다.
- **화면이 «프레임으로만 최신이 되는» 것을 들고 있으면 재접속 목록(`store.tsx` 의 `resync`)에도 넣는다.**
  안 넣으면 맥에서는 멀쩡하고 폰에서만 조용히 낡는다.
- **원격/메인**: 새 설정 줄을 만들 때 «메인에서만 되는가» 를 먼저 정한다.
  안 정하면 원격 화면에 죽은 버튼이 하나 는다 (`Settings.tsx` 의 `device.main` 분기).

## 5. ⚠ 실사고에서 나온 금기

- **클래스 이름 겹침** — 새 클래스를 지을 때 `grep` 으로 먼저 확인한다.
  `.sub`(서브에이전트 행)·`.srow`(세션 행)를 각각 할 일·설정에서 다시 써서 두 번 깨졌다.
- **같은 속성을 애니메이션과 나눠 쓰지 않는다** — 돌고 있는 CSS 애니메이션은 같은 속성의
  일반 선언을 늘 이긴다. `transform` 으로 흔들면서 `transform` 으로 확대할 수 없다
  (`translate`·`scale` 은 독립 속성이라 겹치지 않는다).
- **문턱으로 «키보드가 열렸나» 를 판정하지 않는다** — `innerHeight − vv.height` 는 판마다 0 이 된다.
  판정은 **«입력 중이냐»** 하나뿐이고, 레이아웃도 화장도 같은 판정을 쓴다. 이 문턱은 **네 번** 틀렸다.
- **아이콘 오른쪽 아래 모서리는 상태 배지의 자리다** — 다른 것을 붙이지 않는다(V24 C 안).
