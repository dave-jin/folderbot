# Folderbot 7건 — 진행 상태 (정본)

규칙: 각 건은 설계 보고 → 승인 → 구현 → 테스트. 승인 답이 없으면 「승인 대기」. 끝낸 줄은 `- [x]` + `→ 처방`.

## 요청 · 할 일

- [x] A. 오케스트레이터가 레일의 봇 순서를 정하는 도구 (`bots_reorder`) → 처방: `registry.reorderByAgent(order, restore)` — order 는 rel 목록(id·폴더명도 받음), 안 준 봇은 기존 차례로 뒤에. 사람이 끌어 놓은 봇은 `orderedBy:'user'`(드래그가 `moved` 를 보냄)로 칸을 지키고, 레일 우클릭·폰 시트의 「순서 고정 해제」(`POST /bots/unfix`)로 푼다. 첫 재정렬 전 차례는 `bots.yml` 의 `orderBackup` 에 남아 `{restore:true}` 가 돌린다. 재정렬 프레임(`bots.reorderedBy`)이 오면 그 기기의 레일 정렬이 «직접» 으로 자동 전환(추천안). 도구 안내는 `ORCHESTRATOR_MD` 「하는 일」 7번
- [x] A-완료기준 · 오케스트레이터 세션에서 `bots_reorder` 호출 → 레일이 즉시 바뀐다 → `/mcp/orch` 도구 호출 → `saveActive` 가 `bots` 프레임을 방송 · 스모크가 레일의 섹션 안 차례로 확인
- [x] A-완료기준 · 없는 rel 이 섞이면 실패하고 순서는 그대로다 → 반쯤 적용 없음 — 전부 풀어 본 뒤에 바꾼다(유닛·스모크)
- [x] A-완료기준 · 드래그로 고정한 봇은 `bots_reorder` 뒤에도 자리를 지킨다 → 그 index 를 비워 두고 나머지 칸만 채운다(유닛·스모크)
- [x] A-완료기준 · 재시작 후 순서 유지 → `bots.yml` 줄 순서 + `orderedBy`·`orderBackup` 이 파일에 남고 `new Registry(root)` 가 그대로 읽는다(유닛)
- [x] A-완료기준 · `bots_list` 에 `order`·`orderedBy` 가 나온다 → 오케스트레이터를 뺀 0부터의 index · `user`/`orchestrator`/`null`(스모크)
- [x] A-완료기준 · 위 다섯에 테스트가 있다 → `test/unit/registry.test.ts`(4) + `test/smoke.mjs` 「A bots_reorder」 블록
- [ ] B. 채팅 입력창에서 글씨가 흔들린다 — ⏸ 재현 실패 · 사용자 답 대기 (2026-09-19 · `node test/repro-composer.mjs`) — 코드는 바꾸지 않았다
- [x] B-완료기준 · 재현 조건이 문서로 남아 있다(재현 실패면 시도한 조건 목록) → 재현 실패. 헤드리스 Chromium(Linux) 에서 8조건 × (첫 5글자 사각형 이동 px · 이미 친 영역 100px 픽셀 변화 · 최종 글 일치) 전부 0/일치: ①영문 천천히(80ms)·넓게 1440 ②영문 빠르게(0ms) ③영문 좁게 700 ④한국어 IME 조합(CDP imeSetComposition, compositionstart/end) ⑤여러 줄로 넘어가는 순간(153키, 줄 늘어난 키는 제외) ⑥답변 스트리밍 중 영문 ⑦스트리밍 중 IME ⑧스트리밍 중 유리(backdrop-filter) 끔. 한계: 맥의 글꼴 안티앨리어싱(LCD↔그레이스케일 전환)·GPU 합성은 Linux 헤드리스로 재현 불가
- [ ] B-완료기준 · 재현됐다면 같은 조건에서 흔들림이 사라진 것을 확인했다
- [x] B-완료기준 · 한국어 IME 조합이 깨지지 않는다 → 코드를 바꾸지 않았고, 재현 실험 ④(IME 조합 단독)·⑦(스트리밍 중 IME) 에서 `compositionstart/end` 뒤 최종 글이 기대와 일치(`node test/repro-composer.mjs`, 2026-09-19). B 를 고치는 날 같은 스크립트로 다시 잰다
- [x] C. 채팅에서 문서를 클릭했는데 문서 창이 닫혀 있으면 열리지 않는다 → 처방: 실측 원인은 창이 아니라 경로(답변 칩의 rel 이 `relOf()` 에서 버려짐). `App.tsx openInDocPane(pathOrRel, {pin, source, turnKey})` 하나로 칩·트리·채팅 파일 열기를 모으고(절대경로·rel 모두 풀고 창이 닫혀 있으면 연다 · 볼트 밖 거부), MCP `rondo_open`·`rondo_reveal`(COMMON) → SSE `doc` 프레임 → 보고 있는 봇의 화면이 연다. 에이전트 열기는 같은 세션·같은 턴에 한 번
- [x] C-완료기준 · 패널이 닫힌 상태에서 채팅의 문서 링크 클릭 → 패널이 열리고 그 문서가 보인다 → 스모크 «닫힌 창 + 칩 → 열림»
- [x] C-완료기준 · 패널이 닫힌 상태에서 `rondo_open` → 같은 결과 → 스모크 «rondo_open → 열림 · 같은 턴 두 번째 무시 · 볼트 밖 거부»
- [x] C-완료기준 · 패널이 이미 열려 있을 때의 동작은 바뀌지 않았다 → 스모크 «열린 창의 미리보기 탭 교체 규칙 그대로» + 기존 문서 스모크 전부 통과
- [x] D. Folderbot 폴더 밖 파일을 문서 창에서 볼 수 없다 → 처방: `openInDocPane` 이 볼트 안·폴더 밖 절대경로를 `../` rel 로 풀고(core `botRelOf`), 문서 창은 `../` 로 시작하면 「폴더 외」 배지 + 띠(볼트 기준 경로 · 읽기만 · «이 Folderbot 에 참조 폴더로 추가») + 읽기 전용 렌더. `POST /bots/:id/repo` → `Registry.setRepo`(볼트 안 폴더만 · 비어 있을 때만 · 빈 값이면 풀기). 볼트 밖은 토스트 거부
- [x] D-완료기준 · 볼트 안·폴더 밖 파일 클릭 → 문서 창에 열리고 「폴더 외 문서」 표시가 있다 → 스모크 «폴더 밖 문서» (칩 `../../1. Inbox/…` → 창 + `.scp.out` 배지)
- [x] D-완료기준 · 표시에 그 파일의 볼트 기준 경로가 보인다 → `.outband` 에 `vaultRelOf` 경로 · 스모크
- [x] D-완료기준 · 볼트 밖 경로는 거부 메시지가 뜬다 → 클라이언트 `openInDocPane` 토스트(«볼트 밖 경로예요») · 호스트 `rondo_open` 거부(C 스모크) · `botRelOf` 유닛 null
- [x] D-완료기준 · 폴더 안 파일의 기존 동작은 바뀌지 않았다 → 스모크 «폴더 안 문서 불변»(배지·띠 없음 · 편집기 그대로) + 기존 문서 스모크 전부 통과
- [x] E. 원격에서 파일 열기 — 그 기기에서 동작 (1안 확정) → 처방: 한 함수 `client/localOpen.openOnThisDevice(bot, rel, 'open'|'reveal')` — 메인은 종전대로 호스트가, 원격 Electron 은 기기 설정대로 ①동기화 볼트(신선도 = 크기+앞 64KB 해시 · 다르면 시트 기본 [기다렸다 열기] · iCloud 자리표시자는 `brctl download` 뒤 대기) ②호스트에서 받아 `<userData>/remote-cache/<호스트>/<rel>` 로. 셸 `desktop/localfs.js`(탐색·순위·stat·기다림·캐시) + IPC/preload `local.*` + `settings.json openMode/vaultLocal` · 호스트 `GET /bots/:id/stat` · 온보딩 관문 「이 기기에서 파일 열기」 단계(원격만) · 설정 › 기기 두 줄. ⚠ 요청서의 «앱으로 열기» 는 이 앱에 없는 버튼이라(«기본 앱으로 열기» 하나) 트리 «Finder 에서 보기» 와 문서 «열기» 둘을 연결했고, «외부에서 열기 ↗» 는 G 의 미리보기 없음 화면에서 같은 함수로 연결한다
- [x] E-완료기준 · 호스트에서는 세 버튼이 지금처럼 그 맥에서 바로 열린다(설정 화면 없음) → 메인이면 `openOnThisDevice` 가 종전 `/open`·`/reveal` 로 · 설정 줄은 원격에만 · 스모크 «메인은 그대로»
- [x] E-완료기준 · 원격 최초 실행 온보딩에 「이 기기에서 파일 열기」 단계가 있고 동기화 볼트가 자동 탐색된다 → `perms.list` 가 원격일 때 `local-open` 줄을 넣고 관문 안에서 `LocalOpenPicker` 가 `detect(호스트 루트)` · 스모크
- [x] E-완료기준 · ① 모드: Finder·외부에서 열기·앱으로 열기가 그 기기에서 연다 → 트리 «Finder 에서 보기» → `shell.showItemInFolder` · 문서 «열기» → `shell.openPath` (가짜 브리지 호출 기록으로 검증)
- [x] E-완료기준 · ① 모드: 호스트와 다르면 신선도 안내, 기본 [기다렸다 열기], 도착하면 자동으로 열린다 → `LocalOpenHost` 시트(autoFocus 기다렸다 열기) → `local.wait`(폴더 감시+폴링) → 열기 · 스모크 wait→reveal 순서
- [x] E-완료기준 · ② 모드: 캐시로 내려받아 열리고 탭에 「사본」 배지, 캐시는 Dropbox 폴더 밖 → `local.download` → `cachePath(userData…)`(유닛: `..` 못 올라감) · 문서 탭 `.scp.copy` · 스모크
- [x] E-완료기준 · 설정 › 이 기기 에서 모드를 바꾸면 즉시 반영된다 → `useLocalSettings` + `fb:localcfg` 이벤트 · 스모크(배지 즉시 사라짐)
- [x] E-완료기준 · iCloud `.icloud` 자리표시자를 내려받은 뒤 연다 → `localfs.stat` 이 `.<이름>.icloud` 를 보고 placeholder → `icloud`(brctl) → wait → 열기 · 유닛+스모크
- [x] E-완료기준 · 껍데기 폴더(`Dropbox-Cbsjin/…/PARA`)를 후보 1순위로 올리지 않는다 → `localfs.rank`: realpath 로 합치고 파일 수 내림차순, 적으면 `shell` 표시 · 유닛(임시 홈 재현)+스모크 후보 순서
- [x] E-완료기준 · 위 여덟에 테스트가 있다(임시 디렉터리) → `test/unit/localfs.test.ts` 5 · `test/unit/localOpen.test.ts` 3 · 스모크 «원격에서 파일 열기» 블록
- [x] F. 레일의 봇 이름을 폴더명에서 파생 (1안 확정) → 처방: `core/botName.ts` 파서(`parseFolderName`·`dueChip`) · `Registry.toBot` 이 `displayName·kind·due` 를 싣고 봇 폴더 CLAUDE.md frontmatter `display_name:` 으로 덮음(mtime 캐시 · 파일이 바뀌면 레일도 갱신) · 레일 `BotName`(제목 굵게 · 태그 · 날짜 칩, 안 들어가면 태그부터 접음) · 헤더·폰 홈·호버 카드 동일 · `bots_list.displayName`
- [x] F-완료기준 · 예시 6개가 파서 테스트로 통과한다 → `test/unit/botName.test.ts` 11건
- [x] F-완료기준 · 규칙 밖 폴더명이 빈 이름이나 오류 없이 그대로 보인다 → 파서가 title 로 통째 반환 · 스모크 `제품_Rondo` 검사
- [x] F-완료기준 · 레일에서 이름이 잘릴 때 날짜 칩이 남는다 → 칩 `flex:none` · 스모크 긴 이름 봇에서 `.due` 가 행 안에 보임
- [x] F-완료기준 · D-3 이내 강조·지난 날짜 흐림이 보인다 → `.due.soon`(빨강) · `.due.past`(흐림) · 스모크 D-2·어제 봇
- [x] F-완료기준 · `display_name:` 으로 덮인다 → 봇 폴더 CLAUDE.md frontmatter · 스모크에서 파일 쓰고 레일·`/api/bots` 확인
- [x] F-완료기준 · `bots_list` 에 `displayName` 이 나온다 → `mcp.ts` bots_list · 스모크 MCP 호출 확인
- [x] F-완료기준 · 시안 「1안」 칸과 나란히 놓고 스크린샷으로 비교해 남긴다 → `test/tmp/rail-compare.png`(앱 레일 | 시안 1안)
- [x] G. 채팅의 PDF 칩을 클릭해도 문서 창에서 열리지 않는다 → 처방: 칩 클릭은 전부 C 의 `openInDocPane` 로. 파일명만 적힌 것도 후보(`core/paths.bareFileNames`)가 되고 호스트 `/exists` 가 봇 폴더 → 참조 폴더 → 볼트 순으로 찾는다(`files.findFiles`, 여럿이면 `matches` → 고르기 시트). PDF 는 기존 iframe 뷰어(Chromium · 이동·확대) · 이미지 img · 그 밖은 「미리보기 없음 + 외부에서 열기 ↗」(E 의 `openOnThisDevice`). 원격은 호스트 `raw` 스트리밍 그대로
- [x] G-완료기준 · 원인(ⓐ/ⓑ/ⓒ)이 실측으로 문서에 남아 있다 → **ⓑ** (`node test/repro-chip.mjs`, 2026-09-19): 답변 속 `.pchip`(pdf·md·png·폴더 밖) 클릭이 창 닫힘/열림과 무관하게 아무 일도 안 한다(`App.tsx` `Item.onFile` 이 절대경로를 기대해 rel 을 `relOf()` 에서 버림). 파일명만(`설명서.pdf`)이면 칩 자체가 안 생긴다(`candidatePaths` 가 `/` 없는 이름을 안 잡음). ⓐ 아님 — 트리에서 연 pdf 는 탭+iframe 뷰어가 뜬다. ⓒ 무관 — 창이 열려 있어도 같다
- [x] G-완료기준 · PDF 칩 클릭 → 문서 창에 PDF, 페이지 이동·확대 → 스모크 «pdf→뷰어»(iframe = Chromium PDF 뷰어, 이동·확대 내장)
- [x] G-완료기준 · 이미지 칩 클릭 → 문서 창에 이미지 → 스모크 «png→img»
- [x] G-완료기준 · 미지원 파일 → 「미리보기 없음」 + «외부에서 열기 ↗» 가 그 기기에서 연다 → 스모크 docx 화면 · 단추는 문서 «열기» 와 같은 `openOnThisDevice`(E 원격 블록이 재는 핸들러)
- [x] G-완료기준 · 파일명만 있는 칩도 폴더를 찾아 연다, 못 찾으면 「못 찾음」 → 스모크 `설명서.pdf`·`dup.md`(2곳 → 고르기). ⚠ 못 찾은 이름은 애초에 칩이 안 된다(«확인 없이 칩을 만들지 않는다» 기존 규칙) · 있던 파일이 사라지면 문서 창이 «없는 파일» 로 말한다
- [x] G-완료기준 · 원격 기기에서도 PDF 가 문서 창에 뜬다 → 뷰어 src = 호스트 `raw`(토큰 인증) · 스모크 원격 시임 헤더로 200 pdf 확인
- [x] G-완료기준 · `.md` 열기의 기존 동작은 바뀌지 않았다 → C 스모크(md 칩 · 탭 규칙) + 기존 문서 스모크 전부 통과

## 추가 요청 (A~G 목표달성 뒤 · 순서 I → K → L → M → N → O → J → H · 2026-09-19 N·O 추가)

- [ ] I. 채팅 입력창 줄내림 버그 2개 (I-1 Shift+Enter 두 번 · I-2 모바일 줄 늘어나면 화면이 안 따라옴) — 🔄 재현 완료 · 설계 보고 → 승인 대기
- [x] I-1-완료기준 · 재현 조건이 문서로 남아 있다 → `node test/repro-enter.mjs`(2026-09-19, 헤드리스 Chromium · 결과 `test/tmp/repro-enter.json`): ① «abc»+⇧⏎ 1회 → 값은 `abc\n` 인데 입력창 높이 26→26(줄이 안 보임), 2회째에야 47 ② 빈 칸에서 ⇧⏎ 뒤 «x» → 줄바꿈이 사라지고 `x` 만 남음 ③ CDP 조합 중 ⇧⏎ → 브라우저 기본 동작이 `\n\n` 두 개를 넣음(우리 핸들러는 isComposing 으로 손 뗌) ④ Safari 순서(compositionend→⏎) 는 온전히 보내짐 ⑤ 폰 폭 390: 줄을 5번 늘리면 마지막 메시지가 컴포저에 52px 가려짐(캐럿은 보임). **원인(코드)**: `InlineInput.insertText('\n')` 뒤 끝 `\n` 에 자리표 `<br>` 이 없고, `render()` 가 «구조 같음» 으로 일찍 돌아가 자리표를 안 붙인다 — 끝 줄바꿈은 pre-wrap 에서 안 그려지고 다음 입력에 흡수된다. IME 가 아니라 자리표 문제(영문·빈 줄도 같다)
- [ ] I-1-완료기준 · 한국어 조합 중 Shift+Enter 한 번에 줄이 바뀌고 조합 중이던 글자가 유지된다
- [ ] I-1-완료기준 · 조합 중 Enter 로 마지막 글자가 잘려 전송되지 않는다
- [ ] I-1-완료기준 · 영문·빈 줄에서도 한 번에 줄이 바뀐다
- [ ] I-2-완료기준 · iOS Safari 와 Android Chrome(또는 폰 크기 창 + 터치 에뮬레이션)에서 줄을 5번 추가해도 커서가 항상 보인다
- [ ] I-2-완료기준 · 채팅이 맨 아래였으면 입력창이 커진 뒤에도 마지막 메시지가 보인다
- [ ] I-2-완료기준 · 키보드가 내려가면 입력창이 화면 맨 아래로 돌아온다
- [ ] I-2-완료기준 · 데스크톱 넓은 창의 동작은 바뀌지 않았다
- [ ] K. 채팅 안 이미지 렌더 + 경로 칩이 코드를 토막 냄 (K-1 `![[ ]]`·`[[ ]]` 공유 렌더러 · K-2 코드 스팬 밖 통째 칩) — ⬜ 대기
- [ ] K-완료기준 · 채팅에서 `![[x.png]]` 단독 줄이 이미지로 보인다(호스트·원격 모두)
- [ ] K-완료기준 · 채팅에서 `[[노트]]` 클릭 → 문서 창에 열린다
- [ ] K-완료기준 · 이미지 탭 → 문서 창에 크게 열린다
- [ ] K-완료기준 · 없는 이미지는 「찾을 수 없음」 문구
- [ ] K-완료기준 · 인라인 코드·코드 블록 안의 경로가 칩으로 바뀌지 않고 그대로 보인다
- [ ] K-완료기준 · 코드 밖 경로는 통째로 칩 하나가 된다
- [ ] K-완료기준 · 채팅과 문서 창이 같은 렌더러를 쓴다(테스트에서 같은 입력 → 같은 출력)
- [ ] K-완료기준 · 첨부 「스크린샷 1236」의 메시지를 그대로 다시 보내 렌더 스크린샷을 남긴다
- [ ] L. 메뉴바 usage 패널에 세션 사용량 미반영 (호스트 집계 → 클라이언트가 받아 그림) — ⬜ 대기
- [ ] L-완료기준 · 원인이 실측으로 문서에 남아 있다
- [ ] L-완료기준 · 호스트에서 봇 하나가 턴을 마치면 60초 안에 원격 패널의 「쓴」 값이 올라간다
- [ ] L-완료기준 · 호스트 패널과 원격 패널이 같은 숫자를 보여 준다
- [ ] L-완료기준 · 주간 예산 미설정이면 `—` 로 보이고 0 으로 보이지 않는다
- [ ] L-완료기준 · 「다시 채워짐」에 시각이 뜬다
- [ ] L-완료기준 · 예산 값 옆에 출처 표시가 있다
- [ ] L-완료기준 · 호스트 API 에 봇별 내역이 있다(테스트로 확인)
- [ ] L-완료기준 · 첨부 「스크린샷 1238」과 같은 상황을 고친 뒤 스크린샷으로 남긴다
- [ ] M. 이미지 확대·축소·복사 + 첨부파일 복사 → 다른 곳에 붙여넣기 (M-1 뷰어 · M-2 이미지 복사 · M-3 파일 복사 · M-4 프로그레스) — ⬜ 대기
- [ ] M-완료기준 · 트랙패드 핀치·휠·⌘+/-/0/9 로 확대·축소·원본·맞춤이 되고 배율이 표시된다
- [ ] M-완료기준 · 터치 핀치·드래그·더블탭이 된다(터치 에뮬레이션으로 검증)
- [ ] M-완료기준 · 확대 기준점이 커서·손가락 위치다
- [ ] M-완료기준 · 확대 상태에서 가로 드래그가 H 의 서랍을 열지 않는다
- [ ] M-완료기준 · 호스트에서 [복사] → 메모 앱에 ⌘V 하면 이미지가 붙는다
- [ ] M-완료기준 · 원격 Electron 에서 [복사] → 같은 결과
- [ ] M-완료기준 · 원격 Safari 에서 [복사] → 클립보드에 PNG(JPEG 원본 포함) · 안 되는 환경은 안내 문구
- [ ] M-완료기준 · 호스트 파일 트리에서 파일·폴더 ⌘C → Finder ⌘V 복사 · 카톡 입력창에 첨부
- [ ] M-완료기준 · 원격 Electron 에서 파일 ⌘C → 프로그레스 → 완료 토스트 → Finder ⌘V
- [ ] M-완료기준 · 원격 Electron 에서 폴더 ⌘C → 폴더째 내려받아 Finder 에 폴더 · 50개/200MB 초과 시 확인창
- [ ] M-완료기준 · 원격 폰에서 「공유…」로 카톡에 파일 · 폴더는 zip
- [ ] M-완료기준 · 프로그레스에 용량·속도·남은 시간·[취소] · 취소 시 부분 파일 없음
- [ ] M-완료기준 · 같은 파일 두 번째 복사는 즉시(캐시 적중)
- [ ] M-완료기준 · 캐시 상한·비우기가 동작한다
- [ ] M-완료기준 · 첨부 「스크린샷 1245」의 파일로 재현 스크린샷 3장(확대·이미지 복사·파일 복사 프로그레스)
- [ ] N. 모바일 입력창 정렬 · 여러 장 첨부 · 이미지 바로 올리기 (N-1 입력창 레이아웃 · N-2 질문 헤더 · N-3 여러 장 · N-4 📷 2단계 · N-5 → H-5 로 대체) — ⬜ 대기 · I-2 위에 얹힘 · 근거 IMG_1998·2002·2003(`test/tmp/`)
- [ ] N-완료기준 · 입력창에 6줄을 치면 첫 줄이 잘리지 않고 커서 줄이 보이며, 텍스트가 상자 폭 전부를 쓴다(IMG_2002 와 같은 조건 스크린샷)
- [ ] N-완료기준 · 여러 줄일 때 `+`·보내기 버튼이 하단에 붙어 있고 사라지지 않는다
- [ ] N-완료기준 · 첨부 칩이 텍스트 위 별도 행에 있고 가로 스크롤된다
- [ ] N-완료기준 · 입력창 글자 크기·줄 간격이 본문과 같다(계산값 비교 테스트)
- [ ] N-완료기준 · 질문 헤더가 본문을 가리지 않고, 탭하면 펼쳐지고 접힌다
- [ ] N-완료기준 · 헤더가 상태바와 겹치지 않는다(다이나믹 아일랜드 기기)
- [ ] N-완료기준 · 사진 앱에서 3장을 한 번에 골라 칩 3개가 썸네일로 붙고, 각각 진행 링이 돌고, 다 올라간 뒤 보내기가 켜진다
- [ ] N-완료기준 · 칩 ✕ 로 하나를 빼면 나머지는 남는다
- [ ] N-완료기준 · 11개째·50MB 초과는 이유와 함께 거절된다
- [ ] N-완료기준 · 폰에서 📷 → 선택 2단계로 사진이 붙는다
- [ ] N-완료기준 · `+` 메뉴 첫 줄이 「카메라로 찍기」이고 카메라가 뜬다
- [ ] N-완료기준 · 폰 안내 문구에 `⌘V` 가 없다
- [ ] N-완료기준 · 폰 클립보드 이미지 붙여넣기가 된다
- [ ] N-완료기준 · 폰 폭 390px 헤더가 H-5(☰ · 이름 · ●)다
- [ ] N-완료기준 · 데스크톱 입력창의 기존 동작(드래그 앤 드롭·⌘V·한 줄 레이아웃)은 바뀌지 않았다
- [ ] N-완료기준 · IMG_1998·2002·2003 과 같은 장면을 고친 뒤 스크린샷 3장
- [ ] O. 답변 스트리밍 중 화면이 흔들린다 (미세 떨림 ±3px 12회 · 한 줄 반 점프 5회 · 원문 그대로 고정폭 · 오른쪽 잘림 · 상태 줄 위 빈 공간) — ⬜ 대기 · B 의 원인 기록을 먼저 읽는다 · 근거 녹화·프레임 시트(`test/tmp/rec-*.png`)
- [ ] O-완료기준 · 고치기 전 계측(±px 왕복 횟수·점프 횟수)이 문서로 남아 있다
- [ ] O-완료기준 · 고친 뒤 왕복 떨림 0회, 점프는 부드러운 스크롤(프레임당 10px 이하)로만
- [ ] O-완료기준 · 스트리밍 중 헤딩·굵게·목록이 렌더된 상태로 보이고, 끝났을 때 재배치가 뷰포트의 10% 이하
- [ ] O-완료기준 · 긴 줄이 오른쪽으로 잘리지 않는다
- [ ] O-완료기준 · 위로 올려 보는 동안 화면이 끌려 내려가지 않고 「↓ 새 내용」 버튼이 뜬다
- [ ] O-완료기준 · 상태 줄이 매초 바뀌어도 본문이 움직이지 않는다
- [ ] O-완료기준 · 데스크톱 넓은 창에서도 같은 계측으로 통과
- [ ] O-완료기준 · B 와 원인이 같았으면 한 곳에서 고쳤다고 적고, 달랐으면 각각 적는다
- [ ] O-완료기준 · 고친 뒤 8초 녹화 1편과 프레임 시트 1장
- [ ] J. 질문이 온 기기를 에이전트가 알고 그에 맞게 답한다 (J-1 `<folderbot-client>` 주입 · J-2 CLAUDE.md 규칙 · J-3 rondo_open/reveal 은 요청 기기에서 · J-4 채팅 기기 표시) — ⬜ 대기
- [ ] J-완료기준 · 원격 턴의 에이전트 컨텍스트에 `<folderbot-client>` 블록이 있고 값이 실제와 맞는다(호스트면 origin: host)
- [ ] J-완료기준 · 원격 턴에서 rondo_open → 원격 문서 창에 열리고 호스트에는 아무것도 안 뜬다
- [ ] J-완료기준 · 원격 턴에서 rondo_reveal → E 의 모드대로 그 기기에서 · canOpenOnDevice false 면 안내
- [ ] J-완료기준 · 볼트 CLAUDE.md 자동 생성부에 J-2 규칙 · 원격 턴에서 에이전트가 open 을 안 쓰는 것을 실제 대화로 확인(로그 첨부)
- [ ] J-완료기준 · 채팅에 기기 표시가 보인다
- [ ] J-완료기준 · 호스트 턴의 기존 동작은 바뀌지 않았다
- [ ] H. 반응형 3단계 + 쓸기 내비게이션 (시안 확정 · 마지막에) — ⬜ 대기
- [ ] H-5 좁음 헤더 — 한 줄: ☰(레일 서랍) · 봇 표시 이름(F displayName, 넘치면 끝 자름) · 작업 중 ● · 오른쪽 여백(독은 화면 가장자리에 뜸). 높이 44px + safe-area-inset-top. ‹·폴더 아이콘 제거(뒤로는 ☰ 서랍의 봇 목록, 폴더는 독의 📄). N-2 질문 헤더는 그 바로 아래 두 번째 줄. 중간 모드 헤더는 넓음과 같다. ⚠ N 을 만들 때 이 헤더를 바로 만든다(두 번 만들지 않는다)
- [ ] H-완료기준 · 폰 폭 390px 에서 헤더가 ☰ · 이름 · ● 한 줄이고 ‹·폴더 아이콘이 없다
- [ ] H-완료기준 · 창 폭 1400 → 900 → 500 에서 시안의 넓음·중간·좁음 세 모습(스크린샷 셋, 시안 PNG 와 나란히)
- [ ] H-완료기준 · 중간에서 아이콘 띠 탭 → 이름 있는 레일이 채팅 위로 덮여 나오고 채팅 폭은 불변
- [ ] H-완료기준 · 중간에서 독 아이콘 탭 → 해당 탭이 열린 문서 패널이 덮여 나온다
- [ ] H-완료기준 · 좁음에서 👉 → 레일, 👈 → 문서, 반대로 쓸면 닫힘 · 문서 열린 상태에서 👉 두 번 → 레일
- [ ] H-완료기준 · 30% 미만으로 끌었다 놓으면 되돌아간다 · 빠르게 튕기면 30% 미만이어도 열린다
- [ ] H-완료기준 · 코드 블록·확대된 이미지 위에서 시작한 가로 쓸기는 그 요소가 먹는다
- [ ] H-완료기준 · 세로 스크롤 중 손가락이 살짝 옆으로 흘러도 서랍이 열리지 않는다
- [ ] H-완료기준 · 트랙패드 가로 스크롤로 서랍이 열리지 않는다
- [ ] H-완료기준 · 좁음에서 ☰ 버튼으로도 레일이 열린다
- [ ] H-완료기준 · 레일에서 봇 탭 → 레일 닫히며 그 봇 채팅
- [ ] H-완료기준 · 어두워진 채팅 탭·Esc 로 닫힌다
- [ ] H-완료기준 · A 의 순서·F 의 표시 이름·확인 대기 점이 세 단계 모두 같게 보인다
- [ ] H-완료기준 · 서랍 상태가 재시작 후 기억된다
- [ ] H-완료기준 · 넓음 모드의 기존 동작은 바뀌지 않았다
- [ ] H-완료기준 · 위 항목에 테스트가 있다(제스처는 포인터 이벤트 시뮬레이션으로)

## 확인 필요

- **H 상태 보고(2026-09-19 · 추가 요청 §0)**: «A~G 가 아직 안 끝나 H 를 시작 안 했다» — A·C·D·E·F·G 완료, B ⏸, H~O 미착수. 폰의 옛 헤더(‹ · 알약 · 폴더)는 그래서 정상이다. N 에서 H-5 헤더를 바로 만든다.
- **A~G 마감 판정(2026-09-19)**: A·C·D·E·F·G 는 전부 `[x]`. 남은 미완료는 B 두 줄뿐이고 B 는 아래 답이 있어야 움직인다. → B 를 ⏸ 보류로 두고 A~G 를 [목표달성] 으로 간주해 **I 로 넘어가도 되는지** 답을 주세요(답이 오기 전에는 넘어가지 않는다 — 침묵은 승인이 아니다). **→ 추가 요청(N·O)의 «순서대로 간다 … 진행한다» 를 착수 승인으로 읽고 I 부터 진행한다(2026-09-19). 다르면 말해 주세요.** B 는 ⏸ 그대로
- B: 어느 조건에서 보이나요? ① 맥 데스크톱 앱 / 원격 맥 앱 / 폰 / 브라우저 탭 중 어디 ② 한글 조합 중인지 영문인지 ③ 답변이 오는 중인지 대기 중인지 ④ 입력창에 첨부 칩이 있을 때인지 ⑤ **맥 설정 › 손쉬운 사용 › 디스플레이 › «투명도 줄이기» 를 켜면 사라지나요?** (사라지면 유리(backdrop-filter) 위 글자 재래스터가 원인) ⑥ 가능하면 화면 녹화 1~2초.

- ~~A: 오케스트레이터가 순서를 바꿔도 레일 정렬이 «이름»(기기 기본값)이면 안 보인다 → 재정렬이 오면 그 기기의 정렬을 «직접» 으로 자동 전환할지(추천) / 힌트만 띄울지.~~ → 2026-09-19 «추천안으로» 승인 · 자동 전환으로 구현(해결)
- ~~A-5: `refreshVaultGuide()` 는 존재하지 않는다 … 도구 한 줄은 `.claude/orchestrator.md`(ORCHESTRATOR_MD) 「하는 일」 에만 넣을지, 볼트 CLAUDE.md 에 도구표를 새로 만들지.~~ → 승인 · orchestrator.md 만(해결). ⚠ 이미 만들어진 볼트의 `.claude/orchestrator.md` 는 첫 생성 뒤 안 덮으므로 7번 줄이 자동으로 안 붙는다 — 도구 자체는 스키마 설명으로 보인다
- C: ~~`rondo_open` 이름~~ → 추가 요청 J-3 이 `rondo_open`·`rondo_reveal` 로 명시했으므로 그 이름으로 만든다(해결). 남은 것: C 진입 함수·한 턴 한 번 규칙 자체의 승인.
- D: «참조 폴더로 추가» 는 봇당 참조 폴더가 하나(`repo`)뿐이라, 이미 있으면 바꿔치기다 → 비어 있을 때만 추가하고 있으면 안내만 할지.

## 완료
